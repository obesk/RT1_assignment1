#ifndef CONFIG_H
#define CONFIG_H

//specify the number of turtles you want, this should > 1
#define N_TURTLES 2
//specify the maximum distance that the turtles can have with each oter or the borders
#define ALLOWED_DISTANCE 1.0
//speicify for how much seconds the turtle will move after the velocity input
#define TURTLE_MOVING_TIME 1.0

// this should not be changed
#define BOARD_SIZE 11.0

#endif